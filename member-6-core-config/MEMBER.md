# FINDMYITEM — Member 6: Core config, API client, layout & docs

This folder contains **your part** of the FINDMYITEM team project.

## Your responsibility
The app's backbone: Django project config (settings, URLs, pagination, ASGI/WSGI, manage.py, requirements), the frontend API client, App/main entry, Vite config, the Navbar and BottomNav layout, the Postman collection, and the README.

The files are kept in their real project paths (`backend/...`, `frontend/...`)
so it's clear where they belong in the full app.

> Note: this folder is only your slice, so it will **not run on its own** — it
> plugs into the full app the team assembles. Your job is to own, commit, and
> push these files to your own GitHub account.

## Push this to YOUR GitHub account
1. Create a new **empty** repo on GitHub (e.g. `findmyitem-part-6`) — no README.
2. In this folder, run:
   ```bash
   git init -b main
   git config user.name  "Your Name"
   git config user.email "your-github-email@example.com"
   git add .
   git commit -m "Member 6: Core config, API client, layout & docs"
   git remote add origin https://github.com/<your-username>/findmyitem-part-6.git
   git push -u origin main
   ```
That's it — your work now lives under your own account.
